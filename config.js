// إعدادات موقع مخزني — عدّلها ثم ادفع (push) لتظهر التغييرات.
window.MAKHZANI_CONFIG = {
  // رقم واتساب لاستقبال الطلبات (صيغة دولية بلا + أو 00). 009647771418929 → 9647771418929
  whatsapp: "9647771418929",

  // ── التكامل مع برنامج مخزني عبر Cloudflare Tunnel ──
  // العنوان العام لبرنامج مخزني (من النفق)، مثل: https://xxxx.trycloudflare.com
  // عند ضبطه: تُقرأ الكميات حيّاً من البرنامج، وطلبات الموقع تدخل دورة الطلبيات
  // (STAGING) للموافقة داخل البرنامج مباشرةً. اتركه فارغاً للاعتماد على واتساب فقط.
  apiBase: "",

  // مصدر بديل للكميات إن لم يُستخدم apiBase — Google Sheet منشور (اختياري).
  sheetPubId: "",
  sheetGid: "",

  stockRefreshMs: 90000, // إعادة جلب الكميات كل 90 ثانية (تحديث تلقائي)
};
